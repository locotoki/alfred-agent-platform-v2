services:
  alfred-orchestrator:
    build:
      context: ./services/agent-orchestrator
      dockerfile: Dockerfile
    ports:
      - "8012:8080"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - alfred-network