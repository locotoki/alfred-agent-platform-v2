# Override to disable broken services until they're fixed
services:
  db-storage:
    deploy:
      replicas: 0
    profiles:
      - disabled
  
  db-storage-metrics:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  crm-sync:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  contact-ingest:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  ui-chat:
    deploy:
      replicas: 0
    profiles:
      - disabled

  # Redundant Slack services - replaced by slack-bot v3.1.0
  slack-adapter:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  slack_mcp_gateway:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  echo-agent:
    deploy:
      replicas: 0
    profiles:
      - disabled

  # Excessive database monitoring services
  db-auth-metrics:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  db-api-metrics:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  db-admin-metrics:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  db-realtime-metrics:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  monitoring-db:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  # Broken/incomplete Supabase services
  db-admin:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  db-realtime:
    deploy:
      replicas: 0
    profiles:
      - disabled

  # Duplicate Redis exporter
  monitoring-redis:
    deploy:
      replicas: 0
    profiles:
      - disabled

  # Test/Mock services not needed in production
  hubspot-mock:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  mail-server:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  # Broken UI services
  auth-ui:
    deploy:
      replicas: 0
    profiles:
      - disabled
      
  ui-admin:
    deploy:
      replicas: 0
    profiles:
      - disabled
