# Slack Adapter (stub) 🚧

> Handles inbound/outbound Slack events for `agent-core` and `agent-bizdev`.

## TODO (post-scaffold)
1. Add `/slack/events` handler.
2. Wire `SLACK_BOT_TOKEN`, `SLACK_SIGNING_SECRET` via Doppler.
