[pytest]
markers =
    unit: Unit tests (default)
    integration: Integration tests
    e2e: End-to-end tests that require a running environment

testpaths = tests

python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts = -v --cov=app --cov-report=term-missing