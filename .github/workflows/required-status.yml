name: Required Status Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  required-check:
    runs-on: ubuntu-latest
    steps:
      - name: Always pass
        run: |
          echo "This check always passes and can be used for branch protection rules."
          echo "SUCCESS!"