name: Test Minimal
on:
  push:
    branches: [ci/debug-startup]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo test
        run: echo "Workflow is running!"
