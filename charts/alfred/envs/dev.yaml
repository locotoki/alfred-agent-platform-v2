# Development environment values for Alfred Helm chart
global:
  environment: development

# Metrics are disabled in development
metrics:
  enabled: false

# DB Metrics service configuration (not active in dev)
dbMetrics:
  image:
    repository: db-metrics
    tag: v0.1.0
    pullPolicy: IfNotPresent
